# v8.1 · Audio Bridge（音频桥真正接通）

## 这一版修了什么

v8 的思路是对的：**不去解析网站，直接把播放器的声音拿来识别**——绕开 yt-dlp 解析、DRM、登录、
地址过期这一整类问题。但 v8 里这条链路是"空壳"：

| v8 原状 | 现在 |
| --- | --- |
| `browser_audio_bridge.py` 只有入队函数，队列从来没人消费 | `audio_stream.py` 真正消费 PCM，驱动原有的 ONLINE_* 字幕管线 |
| 没有 `/api/browser-audio/open`：桥推流前无法建立字幕会话 | 新增 open/stop 接口，`/api/companion/open` 增加 `audio_mode` 分支（不做链接解析） |
| 同步接口不告诉页面当前是不是音频模式 | `companion_sync_snapshot` 增加 `audio_mode` 字段 |
| 浏览器端脚本 36 行：硬编码端口、`connect(ctx.destination)` 造成回声、无错误处理 | `BrowserAudioBridge.user.js` 2.0：自动探测端口、零增益输出、AudioWorklet 抓音 + 16k 重采样、丢块保护、面板与字幕条 |
| 队列满了静默丢新块，时间轴悄悄错位 | 改为丢**最旧**块并计数，丢弃量在接口和面板上可见 |
| 版本号混乱（7.6 / 7.7 / v8 混用）、残留 0 字节文件 `Browser` | 全项目统一 v8.1；删除残留文件；音频桥脚本可从面板一键安装 |

## 识别策略（和 v8 设计文档的差别）

设计文档设想"重叠滑动窗口 + 持续修订"。实测发现：短窗口下 Whisper 每窗基本只输出**一条覆盖整窗**的
片段，重叠窗口合并时要么丢文本、要么重复（沙箱实测丢掉了近一半句子）。所以实现改成：

- **不重叠定长窗口**（默认 6 秒，可选 4 / 8），每段只解码一次并追加 —— 结构上不可能丢音频；
- 跨窗口的上下文用 **`initial_prompt` 接续**代替音频重叠；
- 窗口切点落在**语音停顿处**，并多听 0.6 秒、只提交**完整词**（词级时间戳），避免出现 "the brow|ser" 这种断词；
- 用词级时间戳把整窗片段按标点切成**句子级字幕**；
- 静音窗口直接跳过不送 ASR（避免 Whisper 对静音产生幻觉文本）。

## 实测（4 核沙箱，用户 Mac 只会更快）

| 项目 | 结果 |
| --- | --- |
| 端到端（模拟浏览器按真实时间推流 27.4 秒语音） | 6 条字幕，覆盖 0.00→26.87 秒，词覆盖率 **96.5%** |
| 平均延迟 | **4.6 秒**（最快 3.1 秒） |
| 首条字幕出现时间 | 推流开始后约 4 秒 |
| 解码速度（6 秒窗口） | tiny 0.3s / base 0.5s / small 1.2s（RTF 0.06 / 0.08 / 0.20） |
| 停止后可导出 | 实时会话的字幕可导出 SRT |

## 兼容性

- 原「在线字幕」（粘贴链接 + Semantic Look-ahead）和本地视频两条路完全保留，未改动其代码路径；
- Browser Companion（iframe 字幕叠加）保持可用；
- Safari 不支持 `captureStream`，音频桥需要 Chrome / Edge；受 DRM 保护的播放器抓不到声音（会有提示）。
