# 实时字幕（音频桥）· 使用说明

以前这个工具要你先"粘贴视频链接"，让它去解析网站。现在多了一条更直接的路：
**直接抓取当前网页播放器的声音**，边播边识别，字幕叠在画面上——不解析链接、不下载视频、不碰登录。

---

## 一、准备（只需一次）

需要 **Chrome 或 Edge** 浏览器（Safari 不支持这个能力）和 **Tampermonkey** 插件。

1. 双击运行 **AutoSubtitleSync.command**，等窗口显示"已启动"；
2. 在打开的页面右下角找到「**实时字幕（音频桥）**」这张卡片，点「**安装实时字幕脚本**」；
   - 浏览器会打开一段脚本代码。如果装了 Tampermonkey，它会直接弹出安装页；点「安装」即可。
   - 没装 Tampermonkey？去 https://www.tampermonkey.net/ 装好后再点一次。
3. 平时怎么用就怎么用：打开视频网站 → 播放。

## 二、每次使用

1. 确认本机程序还在运行（窗口开着）；
2. 打开视频并**播放几秒**；
3. 点网页右下角的「**实时字幕（音频桥）**」按钮 → 面板里点「**开始实时字幕**」；
4. 大约 3–6 秒后字幕开始逐句出现，之后每 1–2 句刷新一次；
5. 停止：点面板里的「停止」，或关掉标签页（会自动停）。

## 三、面板设置

| 设置 | 说明 |
| --- | --- |
| 识别模型 | `tiny` 最快、`base` 推荐、`small` 最准。模型越大字幕越准，但延迟略高。 |
| 输出字幕 | 想直接看中文就选「中文」（会自动翻译）；选「保持原语言」只出原文。 |
| 字幕快慢 | `快`：延迟更小、长句容易断得碎；`均衡`：推荐；`准`：延迟更大但更准。 |
| 语言检测 | 视频是单一语言时指定该语言更准；多语言混讲保持「自动」。 |
| 双语显示 | 原文 + 翻译同时显示。 |

右下角面板里还能看到「已送入声音 X 秒 / 已识别 Y 条」，字幕条上角显示当前状态和大约慢几秒。

## 四、延迟是怎么回事

实时字幕后端是"攒一小段声音 → 识别 → 定稿"：所以字幕总是比说话慢几秒（默认约 5–8 秒，选「准」会更慢）。
这是准确率和延迟之间的取舍，越短越容易听错。**字幕显示的是最新一句**（而不是像看回放那样跟着进度条走）。

## 五、常见问题

- **提示"这个浏览器不能直接抓音频"**：你用的是 Safari。换 Chrome / Edge。
- **提示"抓不到声音"**：该播放器受 DRM / 跨域保护（常见于 Netflix 等），浏览器不允许取声音。
  改用原来的「在线字幕」模式（粘贴链接）试试。
- **字幕断断续续 / 提示丢弃音频块**：电脑处理不过来（模型太大、同时开太多程序）。
  把模型调成 `base` 或 `tiny`，关闭多余程序。
- **拖动进度条后字幕乱掉**：跳转会自动重开一次会话（字幕重新开始），属于正常行为。
- **切到别的标签页后字幕停了**：浏览器会限制后台标签页的音频处理能力，把视频页面切到前台即可。
- **同时装了"浏览器 Companion"脚本**：两者都会在画面上叠字幕，用哪个就只启动哪个。

## 六、隐私

声音只发到你自己电脑上的 `127.0.0.1`（本机服务），不经过任何第三方服务器；也不会保存音频文件（识别完即丢）。
