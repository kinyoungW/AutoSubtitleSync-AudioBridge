# 实时字幕（音频桥）· 使用说明

以前这个工具要你先"粘贴视频链接"，让它去解析网站。现在多了一条更直接的路：
**直接抓取当前网页播放器的声音**，边播边识别，字幕叠在画面上——不解析链接、不下载视频、不碰登录。

---

## 一、准备（只需一次，这一步最容易卡住）

实时字幕必须要一个浏览器插件 **Tampermonkey**（用户脚本管理器）；浏览器要 **Chrome 或 Edge**（Safari 不支持抓播放器的声音）。

### 1) 先装 Tampermonkey

- **推荐用 Edge**：Edge 的加载项商店国内可以直接访问 → 商店里搜 `Tampermonkey` → 添加。
  （Chrome 的应用商店在国内经常打不开，要代理才行；打不开就换 Edge，功能完全一样。）
- 装完**确认它是启用状态**：地址栏输入 `edge://extensions`（Chrome 是 `chrome://extensions`）。
  装了但被禁用，效果等于没装——后面会报同样的错。

### 2) 再把脚本装进去：三种方法，任选

1. **拖进去（最稳）**：把 `BrowserAudioBridge.user.js` 这个文件**直接拖进浏览器窗口**，Tampermonkey 会弹出安装页 → 点「安装」。
2. **从文件导入**：点工具栏的 Tampermonkey 图标 → 「管理面板」→ 左侧「实用工具」→「从文件导入 / Import from file」→ 选中那个 `.user.js`。
3. **粘贴（一定能成）**：管理面板 → 左栏「+」新建脚本 → 全选删掉模板内容 → 把 `.user.js` 的内容粘进去 → `Cmd + S` 保存。

> 不推荐的做法：点脚本的网址链接。Chromium 已经不允许从网页直接安装用户脚本了，
> 点了多半会出现 **"Apps, extensions, and user scripts cannot be added from this website"**。

### 3) 那句英文报错是什么意思

看到 **"Apps, extensions, and user scripts cannot be added from this website"**，说明 **Tampermonkey 没装、或者装了但没启用**，
不是脚本坏了。按上面 1、2 步做就行。注意：本机面板里那个「安装实时字幕脚本」按钮走的正是这条链接路，
所以**在装好 Tampermonkey 之前，点它一定会报这句话**——正常现象。

### 4) 怎么确认装好了

打开任意视频网站，播放几秒 → 页面右下角出现「**实时字幕（音频桥）**」小按钮 = 成功。

没出现的话依次检查：

1. Tampermonkey 是否启用（`edge://extensions`）；
2. Tampermonkey 里这条脚本是否启用（管理面板里脚本名左边的开关是绿的）；
3. **站点访问权限**：在 `edge://extensions` 里把 Tampermonkey 的「站点访问 / 允许此扩展程序」设为**在所有网站上**；
   如果它提示需要打开「开发者模式」，按提示打开（这是 Chrome 对用户脚本扩展的权限要求，不影响安全）；
4. 刷新视频页面（脚本是页面加载时注入的，装完必须刷新）。

### 5) 暂时不想装插件？

实时字幕本身离不开插件。但如果你只是想**先测某个网站能不能用影子播放器**，
可以直接用控制台版：打开视频播放几秒 → `Option + Command + J` 开控制台 →
**首次粘贴需按提示手打 `allow pasting` 回车** → 把 `tests/probe_console.js` 的内容粘进去回车。
原理和面板里那个按钮一样，只是每次要手动跑一次。

## 二、每次使用

1. 确认本机程序还在运行（窗口开着）；
2. 打开视频并**播放几秒**；
3. 点网页右下角的「**实时字幕（音频桥）**」按钮 → 面板里点「**开始实时字幕**」；
4. 大约 3–6 秒后字幕开始逐句出现，之后每 1–2 句刷新一次；
5. 停止：点面板里的「停止」，或关掉标签页（会自动停）。

## 三、抓音来源怎么选（3.0 新增）

面板第一项就是「抓音来源」，三种各有取舍：

| 来源 | 能抓到什么 | 抓不到的 | 需要准备 |
| --- | --- | --- | --- |
| **播放器元素**（默认） | 页面里 `<video>/<audio>` 的声音；只有它支持「影子播放器」前瞻 | 跨域 / `blob:`(MSE) / 用 WebAudio 播的播放器 | 无 |
| **共享标签页音频** | 这个标签页最终输出的声音：**iframe、MSE、blob、跨域、WebAudio 全通吃** | Safari 不支持；DRM 保护的内容可能只有静音 | 点一次浏览器弹窗：选「当前标签页」并勾上「分享标签页音频」 |
| **系统声音** | **电脑正在播放的任何声音**（不分浏览器、不分网站、连 DRM 播放的也算） | —— | 装一次虚拟声卡 BlackHole（见下） |

选择建议：**先试「播放器元素」**（最准，还能前瞻）；出现「抓不到声音」或播放器在 iframe 里找不到，就换「共享标签页音频」；想连 Netflix 这类都吃下来，用「系统声音」。

### 播放器在 iframe 里怎么办

不用管——脚本在每个框架里都会跑，但只有顶层显示面板。如果检测到播放器在子框架里，面板会提示「检测到播放器在子框架里，开始时会自动交给那边抓音」，点开始即可，字幕会叠在播放器上；探测报告也会自动转交过去、再把结果回传到面板显示。

（唯一的小坑：子框架里如果没有过点击，浏览器会拦着不让取音。所以若开始后提示「这一帧还差一次点击才能开始抓音」，在视频画面上点一下就行。）

### 系统声音：装一次 BlackHole（可选，最万能）

1. 安装虚拟声卡：`brew install blackhole-2ch`，或到 https://existential.audio/blackhole/ 下载 pkg 安装；
2. 打开「音频 MIDI 设置」→ 左下角 `+` → **创建多输出设备** → 勾选 **BlackHole 2ch** 和你的扬声器/耳机 → 把这台多输出设备**设为系统输出**（这样声音同时进 BlackHole 和扬声器，你自己照常听得到）；
3. 回到面板，抓音来源选「系统声音」，点开始。第一次会弹麦克风权限，允许即可（系统设置 → 隐私与安全性 → 麦克风 → 勾选终端）；
4. 面板会显示正在采哪个设备。如果抓的不是 BlackHole，说明它没被识别成推荐设备，可在本机控制台查看 `/api/system-audio/devices` 的输出挑选。

## 四、面板设置

| 设置 | 说明 |
| --- | --- |
| 识别模型 | `tiny` 最快、`base` 推荐、`small` 最准。模型越大字幕越准，但延迟略高。 |
| 输出字幕 | 想直接看中文就选「中文」（会自动翻译）；选「保持原语言」只出原文。 |
| 字幕快慢 | `快`：延迟更小、长句容易断得碎；`均衡`：推荐；`准`：延迟更大但更准。 |
| 语言检测 | 视频是单一语言时指定该语言更准；多语言混讲保持「自动」。 |
| 双语显示 | 原文 + 翻译同时显示。 |

右下角面板里还能看到「已送入声音 X 秒 / 已识别 Y 条」，字幕条上角显示当前状态和大约慢几秒。

## 五、延迟是怎么回事

实时字幕后端是"攒一小段声音 → 识别 → 定稿"：所以字幕总是比说话慢几秒（默认约 5–8 秒，选「准」会更慢）。
这是准确率和延迟之间的取舍，越短越容易听错。**字幕显示的是最新一句**（而不是像看回放那样跟着进度条走）。

## 六、常见问题

- **提示"这个浏览器不能直接抓音频"**：你用的是 Safari。换 Chrome / Edge。
- **提示"抓不到声音"**：该播放器受 DRM / 跨域保护（常见于 Netflix 等），浏览器不允许取声音。
  改用原来的「在线字幕」模式（粘贴链接）试试。
- **字幕断断续续 / 提示丢弃音频块**：电脑处理不过来（模型太大、同时开太多程序）。
  把模型调成 `base` 或 `tiny`，关闭多余程序。
- **拖动进度条后字幕乱掉**：跳转会自动重开一次会话（字幕重新开始），属于正常行为。
- **切到别的标签页后字幕停了**：浏览器会限制后台标签页的音频处理能力，把视频页面切到前台即可。
- **提示「没有发现虚拟声卡」**：用「系统声音」要先按第三节装一次 BlackHole，并把系统输出设成多输出设备。
- **同时装了"浏览器 Companion"脚本**：两者都会在画面上叠字幕，用哪个就只启动哪个。
- **装了 Tampermonkey，但页面右下角没有那个按钮**：多半是"站点访问权限"没放开或页面没刷新——见第一节第 4 步的排查清单。


## 七、隐私

声音只发到你自己电脑上的 `127.0.0.1`（本机服务），不经过任何第三方服务器；也不会保存音频文件（识别完即丢）。

## 八、「测试影子播放器」按钮（给字幕加前瞻）

面板里有一个 **「测试影子播放器（可选）」** 按钮。它做的事：在**同一个页面里**克隆一个静音播放器，
跳到比当前播放位置**提前 60 秒**的地方播放同一段视频，然后测三件事——能不能建起来、能不能跳进去、
抓到的声音有没有声。

如果能成，识别就拿到了"未来 60 秒的声音"，断句能像导入字幕那样自然，字幕也能按进度对齐显示
（不再需要"永远显示最新一句"）。代价是这个页面会同时拉两路流。

点完按钮会出一份报告，报告会告诉你在**当前这个网站**上能不能用。注意：

- 只对"地址是普通 http(s) 视频/流"的网站有效；用 `blob:` 喂数据的（多数大站）和 DRM 保护的不行；
- 内容是直播的没有"未来"，跳不进去；
- 会同时播两路，部分站点可能把播放进度写进观看历史。

测完把报告发给 Claude，就能确定要不要按这条路做下去。
